Start 
```docker-compose up -d```

Row data in database
```npm run script```
if first time start, use this before start script
```npm install``` 

main env path 
```app/env/.env```
example env path
```env/env/debug.env```
